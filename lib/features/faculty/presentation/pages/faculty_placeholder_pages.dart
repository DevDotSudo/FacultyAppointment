import 'package:flutter/material.dart';
import '../../../../core/theme/app_colors.dart';

// Empty placeholder widgets for future pages

class FacultyProfilePage extends StatelessWidget {
  const FacultyProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Faculty Profile', style: TextStyle(color: AppColors.textPrimary))),
    );
  }
}

class ManageAvailabilityPage extends StatelessWidget {
  const ManageAvailabilityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Manage Availability', style: TextStyle(color: AppColors.textPrimary))),
    );
  }
}

class RequestDetailPage extends StatelessWidget {
  const RequestDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Request Detail', style: TextStyle(color: AppColors.textPrimary))),
    );
  }
}

class AppointmentRequestsPage extends StatelessWidget {
  const AppointmentRequestsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Appointment Requests', style: TextStyle(color: AppColors.textPrimary))),
    );
  }
}

class MyAppointmentsPage extends StatelessWidget {
  const MyAppointmentsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('My Appointments', style: TextStyle(color: AppColors.textPrimary))),
    );
  }
}

class BookAppointmentPage extends StatelessWidget {
  const BookAppointmentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Book Appointment', style: TextStyle(color: AppColors.textPrimary))),
    );
  }
}

class FacultyListPage extends StatelessWidget {
  const FacultyListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Faculty List', style: TextStyle(color: AppColors.textPrimary))),
    );
  }
}

class StudentProfilePage extends StatelessWidget {
  const StudentProfilePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Student Profile', style: TextStyle(color: AppColors.textPrimary))),
    );
  }
}

class AppointmentDetailPage extends StatelessWidget {
  const AppointmentDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Appointment Detail', style: TextStyle(color: AppColors.textPrimary))),
    );
  }
}
